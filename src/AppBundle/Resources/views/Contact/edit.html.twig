{% extends 'AppBundle::template.html.twig' %}

{% block title %} Contact edit - DWH CRM {% endblock %}

{% block body -%}
    <h1>Contact edit</h1>

    {{ form(edit_form) }}

<ul class="record_actions">
    <li>
        <a href="{{ path('contacts_list') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
